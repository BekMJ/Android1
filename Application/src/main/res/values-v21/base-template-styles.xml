<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme using Material Components -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/gray</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="android:windowBackground">@color/backgroundLight</item>
        <!-- Optional: Default text color -->
        <item name="android:textColor">@color/textPrimary</item>
    </style>

    <!-- Custom button style using Material style attributes -->
    <style name="CustomButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:textColor">@color/colorTitle</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Header and body text styles -->
    <style name="HeaderText" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">8dp</item>
    </style>

    <style name="BodyText" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:padding">8dp</item>
    </style>

</resources>
